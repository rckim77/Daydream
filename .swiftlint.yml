disabled_rules:
 - trailing_whitespace
 - identifier_name
 - function_body_length
 - type_body_length
excluded: # paths to ignore during linting. Takes precedence over `included`.
 - Pods
 - DaydreamUITests
line_length: 158
